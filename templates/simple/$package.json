{
  "name": "storefront-<%= appName %>",
  "private": true,
  "description": "Simple StoreFront implementation",
  "scripts": {
    "start": "live-server --port=6500 --host=localhost"
  },
  "author": "Groupby Inc.",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.0"
  }
}
